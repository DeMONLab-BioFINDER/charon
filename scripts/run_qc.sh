#!/bin/bash
# run_qc.sh — not yet implemented
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/utils/logging.sh"
log_warn "run_qc.sh is not yet implemented — skipping"
exit 0
